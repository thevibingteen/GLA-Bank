# MongoDB connection string (Atlas recommended)
MONGODB_URI="mongodb+srv://<username>:<password>@cluster0.mongodb.net/glabank?retryWrites=true&w=majority&appName=GLABank"

# JWT settings
JWT_SECRET=change-this-to-a-strong-secret
JWT_EXPIRES_IN=7d

# Backend port
PORT=5000

# Frontend origin for CORS (optional in development)
FRONTEND_URL=http://localhost:5173
